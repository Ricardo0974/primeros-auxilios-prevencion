services:
  backend:
    build: ./Backend
    container_name: backend
    ports:
      - "8000:8000"

  frontend:
    build: ./frontEnd
    container_name: primeros-auxilios-prevencion
    ports:
      - "8082:8082"

    depends_on:
      - backend